IO.puts("====-====-====-====-====")
IO.puts(".iex.exs")

IO.puts("* Import Modules")
import Ecto.Query
import Jehuty

IO.puts("* Alias Modules")
alias Jehuty.Repo
alias Schemas.Account.User
# alias Queries.AccountQuery

IO.puts("====-====-====-====-====")
